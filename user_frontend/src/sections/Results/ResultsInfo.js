import Container from '@mui/material/Container'
import Card from '@mui/material/Card'
import CardContent from '@mui/material/CardContent'
import Grid from '@mui/material/Grid'
import { useEffect, useState } from 'react'
import SubmitComplainModal from '../../components/Modal/SubmitComplainModal'
import Button from '@mui/material/Button'

export default function ResultsInfo(props) {

    const {
        election_id,
        results,
        handleOpenModal
    } = props



    return (<Card style={{ borderRadius: 10, border: '1px solid #E4E4E4', boxShadow: 'none' }}>

        <CardContent >

            <Container>
                <Grid container >

                    <Grid container item xs={12} md={8} lg={8} justifyContent={'start'} style={{ textAlign: 'start' }}>

                        <Grid item container>

                            <p><span style={{ fontWeight: 'bold' }}>Election id</span>: {election_id}</p>
                        </Grid>
                        <Grid item container>

                            <p><span style={{ fontWeight: 'bold' }}>Number of votes</span>: {results !== null? results.no_of_votes: '---'}</p>
                        </Grid>
                        <Grid item container>
                            <p><span style={{ fontWeight: 'bold' }}>Number of voters</span>: {results !== null? results.no_of_voters: '---'}</p>
                        </Grid>

                    </Grid>


                    <Grid container item xs={12} md={4} lg={4} justifyContent={'center'} alignContent={'center'}>


                        <Grid item container>

                            <p><span style={{ fontWeight: 'bold' }}>Date generated</span>: {results !== null? results.date_generated: '---'}</p>
                        </Grid>
                        <Grid item container>
                            <p><span style={{ fontWeight: 'bold' }}>Generated by</span>: {results !== null? "Admin": '---'}</p>
                        </Grid>

                        <Grid item container>
                            <p><span style={{ fontWeight: 'bold' }}>Winner</span>: {results !== null? results.first_candidate_name: '---'}</p>
                        </Grid>


                        <Grid item container>
                        <Button size={'large'} disabled={results === null? true: false} onClick={handleOpenModal} style={{ backgroundColor: '#004378', color: 'white', textTransform: 'none' }}>Submit complain</Button>
                        </Grid>
                    </Grid>


                </Grid>
            </Container>

        </CardContent>

    </Card>)
}